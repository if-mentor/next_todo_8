import BaseButton from "./BaseButton";

const RestoreAll = () => {
  return (
    <BaseButton backgroundColor={'blue.200'} fontColor={'white'} borderColor={'white'}>Restore All</BaseButton>
  );
};

export default RestoreAll;
