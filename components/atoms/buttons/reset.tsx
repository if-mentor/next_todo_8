import BaseButton from "./BaseButton";

const Reset= () => {
  return (
    <BaseButton backgroundColor={'blackAlpha.400'} fontColor={'black'} borderColor={'black'}>RESET</BaseButton>
  );
};

export default Reset;
